<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Zgłoska" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="bg-orange-50 text-gray-900 min-h-screen flex flex-col">

    <header class="bg-white shadow flex justify-center mb-10">
      <img src="/logo.png" alt="Zgłoska" class="h-16 w-auto object-contain" />
    </header>

    <div class="container mx-auto px-4 mt-4">
      <% if notice %>
        <div class="bg-green-100 border border-green-300 text-green-800 px-4 py-3 rounded mb-4">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div class="bg-red-100 border border-red-300 text-red-800 px-4 py-3 rounded mb-4">
          <%= alert %>
        </div>
      <% end %>
    </div>

    <main class="flex-grow container mx-auto px-4 py-6">
      <%= yield %>
    </main>

  </body>
</html>
